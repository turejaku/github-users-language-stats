This project is meant to list public repositories of chosen GitHub user, and show statistics of programming languages used in them.

Note that only small number of request per hour is possible, as this application doesn't do any kind of authentication (this limits possible request to GitHub API).
