This project is meant to list public repositories of chosen GitHub user, and show statistics used in them.

Note that only small number of request per hour is possible, as this application doesnt do any kind of authentication (this limits possible request to GitHub API).
